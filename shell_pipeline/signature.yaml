version: 1.2

sig:extra_pip_packages: owl-shell-pipeline
sig:active: true

name: shell

command: |
  sleep 300

# use dask (optional, see docs)
# use_dask: false

# output directory (optional)
# - sets the directory where the script is run
# - stores pipeline logs
# output: /storage/user/output

resources:
  workers: 1
  memory: 1
  cores: 1
